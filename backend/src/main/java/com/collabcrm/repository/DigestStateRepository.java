package com.collabcrm.repository;

import com.collabcrm.model.DigestState;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DigestStateRepository extends JpaRepository<DigestState, String> {
}
